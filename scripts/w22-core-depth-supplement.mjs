export const W22_CORE_SUPPLEMENTS = {
  'ai-agent': `**적합한 자동화 범위 고르기**

에이전트가 적합한 업무는 목표까지 여러 판단이 필요하고 중간 결과에 따라 다음 행동이 달라지지만, 각 행동의 성공 여부를 관찰할 수 있는 경우다. 반대로 입력과 출력 규칙이 안정적이고 단계가 고정된 업무는 일반 워크플로나 프로그램이 더 저렴하고 예측 가능하다. 자유로운 에이전트와 고정 자동화 사이에는 모델이 계획만 제안하는 방식, 승인된 도구 중 하나를 고르는 방식, 사람이 승인한 단계만 실행하는 방식이 있다. 필요한 자율성의 최소 수준을 선택하면 시험 공간과 피해 범위를 줄일 수 있다.

후보 업무를 평가할 때는 완료율만 보지 않고 사람이 절약한 시간, 수정에 든 시간, 잘못된 외부 행동의 비용과 감사를 위한 운영 비용을 포함한다. 예외가 많다는 이유만으로 에이전트가 유리한 것도 아니다. 예외를 판단할 충분한 상태가 도구로 제공되는지, 성공을 기계적으로 확인할 수 있는지, 실패 뒤 되돌릴 수 있는지가 먼저다. 정보 검색처럼 결과를 검토할 수 있는 과제에서 시작해 읽기 전용 도구와 제한된 데이터로 증거를 쌓고, 동일 실패가 반복되지 않는 통제가 확인될 때만 권한을 넓힌다.`,
  api: `**API 형식 선택 기준**

동기식 요청·응답은 결과가 빠르게 끝나고 호출자가 연결을 유지할 수 있을 때 단순하다. 시간이 오래 걸리거나 재처리가 필요한 작업은 작업 식별자를 반환한 뒤 상태를 조회하는 비동기 패턴이 적합하다. 서버가 사건을 밀어줘야 하면 웹훅, 서버 전송 이벤트, 웹소켓을 고려하지만 전달 보장과 재연결, 순서, 서명 검증을 별도로 설계한다. 대량 데이터는 수천 번의 작은 호출보다 배치나 스트리밍이 효율적일 수 있다. 형식은 유행이 아니라 데이터 크기, 지연 요구, 실패 복구와 소비자 환경으로 결정한다.

조직 내부 API도 외부 API와 같은 계약 규율이 필요하다. 네트워크 경계가 내부라고 호출자가 동시에 배포되거나 신뢰할 수 있다는 뜻은 아니다. 공용 스키마와 SDK가 편리해도 특정 언어 구현에 서버 의미가 묶이지 않게 하고, 원시 HTTP 계약을 확인할 수 있어야 한다. 생성형 AI 모델을 소비자로 연결할 때는 작은 목적별 연산, 명확한 필드 설명, 제한된 열거형과 건조 실행 기능이 도구 선택 오류를 줄인다. 하나의 거대한 “모든 작업” 엔드포인트는 권한과 부작용을 구분하기 어렵게 만든다.`,
  'model-hub': `**공개 허브와 내부 레지스트리의 역할**

공개 허브는 폭넓은 후보 발견, 연구 결과 공유와 생태계 도구 연결에 강점이 있다. 내부 레지스트리는 조직이 검토한 리비전, 접근 권한, 배포 이력과 규제 증거를 통제하는 데 유리하다. 둘 중 하나만 선택할 필요는 없다. 외부 후보를 격리된 수집 단계에서 가져와 파일·라이선스·행동 평가를 거친 뒤, 승인된 불변 아티팩트만 내부 레지스트리로 승격하는 구조가 가능하다. 운영 서비스가 외부의 이동하는 기본 브랜치를 직접 참조하지 않으면 삭제·변조·네트워크 장애에 대한 의존도도 줄어든다.

모델 공유의 가치는 재현 가능한 맥락이 있을 때 커진다. 가중치만 올리고 토크나이저, 전처리, 평가 코드와 학습 기반을 누락하면 다른 사용자가 같은 결과를 만들 수 없다. 반대로 모든 원천 데이터를 공개할 수 없는 경우에는 공개할 수 없는 이유, 데이터 범주와 시점, 제외 정책과 알려진 결손을 구체적으로 설명한다. 조직은 다운로드 인기보다 카드 완결성, 불변 리비전, 안전한 직렬화, 재현된 평가와 지원 가능한 실행 환경에 가중치를 두어 후보를 순위화할 수 있다.`,
  evaluation: `**평가 방법을 과제에 맞추기**

정답이 하나인 분류·추출은 정확 일치와 클래스별 지표가 유용하지만, 자유 형식 생성은 핵심 사실, 근거, 형식, 유해성을 별도 기준으로 나누는 편이 낫다. 검색 시스템은 최종 답변만 보면 검색 실패와 생성 실패를 구분할 수 없으므로 후보 회수율, 재순위와 근거 사용을 단계별로 측정한다. 에이전트는 최종 상태뿐 아니라 도구 호출의 유효성, 불필요한 행동, 승인 우회와 복구 능력을 본다. 코드 생성은 테스트 통과에 더해 보안, 성능, 기존 동작 회귀를 확인한다.

온라인 A/B 시험은 실제 사용 결과를 보여 주지만 사용자에게 위험을 노출할 수 있고 계절성·학습 효과·트래픽 구성의 영향을 받는다. 오프라인 평가로 명백한 퇴행과 안전 문제를 먼저 제거하고, 작은 트래픽과 중단 조건으로 온라인 증거를 보완한다. 클릭과 체류 시간은 만족이나 정확성의 완전한 대리 지표가 아니므로 사용자 수정, 업무 완료, 불만과 장기 효과를 함께 본다. 고위험 영역에서는 온라인 실험보다 시뮬레이션, 전문가 검토와 관찰 전용 배포를 우선한다.`,
  'artificial-intelligence': `**접근법 선택과 혼합**

지식과 제약이 명확하고 오류 허용이 낮은 영역에서는 규칙, 검색, 최적화와 검증 가능한 계산이 중심이 될 수 있다. 이미지·음성·언어처럼 규칙을 모두 작성하기 어려운 패턴에는 학습 모델이 유용하다. 생성 모델은 열린 표현을 만들지만 정확한 산술, 최신 상태와 권한 판단을 외부 도구에 맡길 수 있다. 여러 접근을 결합할 때는 각 구성 요소가 실패하면 무엇이 관찰되는지, 어느 단계에서 대체 경로로 전환하는지 정의한다. 모델이 모든 결정을 맡는 구조보다 검증 가능한 부분을 코드와 데이터베이스에 두는 편이 재현성과 책임성을 높인다.

도입 우선순위는 기술 시연의 인상보다 문제의 반복 빈도, 데이터 준비도, 오류의 영향, 사람 검토 가능성과 운영 역량으로 정한다. 업무 시간이 많이 들더라도 사례가 드물고 정답 확인이 어렵다면 학습과 평가 데이터를 만들 비용이 이익보다 클 수 있다. 반대로 높은 빈도의 저위험 분류와 보조 작성은 사람 수정 기록을 통해 점진적으로 개선할 수 있다. 시스템의 영향을 받는 사용자와 현장 담당자를 문제 정의와 실패 검토에 포함해야 개발자가 예상하지 못한 비용과 우회 행동을 발견할 수 있다.`,
  inference: `**지연·처리량·비용의 균형**

실시간 대화는 첫 토큰 지연과 토큰 간 간격이 중요하고, 야간 문서 처리는 전체 처리량과 단가가 더 중요하다. 같은 모델도 온라인 큐와 오프라인 배치를 분리하면 서로 다른 목표에 맞게 자원을 활용할 수 있다. 출력 길이는 생성 비용과 사용자가 기다리는 시간을 직접 늘리므로 최대 토큰만 크게 잡기보다 과제별 종료 조건을 둔다. 긴 입력을 무조건 모델에 넣지 말고 중복 제거, 검색, 구조화된 상태로 필요한 문맥을 선택한다. 이 과정에서 품질 손실이 없는지는 실제 질의로 측정한다.

하드웨어 선택은 이론 연산량뿐 아니라 가중치와 KV 캐시가 메모리에 들어가는지, 필요한 자료형과 커널이 지원되는지, 장치 간 통신과 운영 가용성을 본다. 소형 모델을 더 많은 복제본으로 제공하는 방식과 대형 모델을 병렬화하는 방식의 비용 구조가 다르다. 요청을 난이도에 따라 라우팅하거나 캐시를 사용하면 평균 비용을 줄일 수 있지만 잘못된 라우팅과 오래된 결과의 위험이 생긴다. 비용 보고에는 유휴 용량, 모델 적재, 재시도와 실패 요청도 포함한다.`,
  'language-model': `**모델 유형과 사용 방식 선택**

n-그램과 작은 전용 모델은 제한된 어휘와 낮은 지연, 해석 가능한 기준선이 필요한 과제에서 여전히 가치가 있다. 대형 생성 모델은 여러 과제를 자연어 지시로 다룰 수 있지만 계산 비용과 출력 변동, 검증 부담이 크다. 분류나 정보 추출처럼 출력 공간이 제한된 문제는 전용 판별 모델과 생성 모델을 모두 기준선에 두고 데이터 양, 변경 빈도, 지연과 오류 비용을 비교한다. 로컬 배포와 외부 API도 개인정보, 제어, 운영 인력, 확장성과 업데이트 속도의 균형이 다르다.

모델 자체 지식만 사용할지 검색 증강과 도구를 연결할지는 정보의 최신성, 출처 요구와 계산 가능성에 달려 있다. 사내 문서 질의는 접근 권한을 보존한 검색이 필요하고, 산술·일정·재고는 권위 있는 시스템을 조회하는 편이 낫다. 긴 문서를 프롬프트에 넣는 방식과 관련 부분을 검색하는 방식은 누락과 비용이 다르므로 같은 평가 세트로 비교한다. 작은 모델이 기준을 충족하면 크기보다 운영 단순성을 선택할 수 있으며, 어려운 입력만 큰 모델로 보내는 경로는 라우팅 오류를 별도 평가해야 한다.`,
  vector: `**표현과 거리의 선택**

범주형 값은 임의의 정수 크기로 표현하면 존재하지 않는 순서가 생길 수 있어 원-핫 벡터나 학습 임베딩을 사용한다. 물리량은 단위와 척도를 맞추고, 각도처럼 주기적인 값은 사인·코사인 성분으로 표현할 수 있다. 텍스트의 단어 빈도는 희소 벡터, 의미 검색은 학습된 밀집 벡터가 후보지만 두 표현은 보존하는 정보가 다르다. 희소 검색은 정확한 고유명사와 숫자에 강하고 밀집 검색은 재서술에 강할 수 있어 결합 검색이 유리한 경우가 있다.

차원 축소는 시각화, 노이즈 제거, 계산 절약에 쓰이지만 보존하려는 구조를 명시해야 한다. PCA는 큰 분산 방향을 선형적으로 보존하고 비선형 시각화 기법은 국소 이웃을 강조할 수 있으나 그림의 전역 거리까지 의미 있게 만들지는 않는다. 모델 입력의 벡터 표현을 고를 때는 다운스트림 지표와 자원으로 비교하고, 시각적으로 군집이 예쁘다는 이유만으로 선택하지 않는다. 축을 추가하거나 순서를 바꿀 때 스키마 버전을 올려 학습된 파라미터와 잘못 결합되지 않게 한다.`,
  'image-classification': `**분류가 적합한 문제인지 판단하기**

이미지 전체의 주된 범주만 필요하면 분류가 단순하고 효율적이다. 한 이미지에서 여러 물체의 위치가 필요하면 객체 탐지, 각 화소의 영역이 필요하면 분할, 시각과 문장의 관계가 필요하면 멀티모달 검색이나 질의응답이 더 적합하다. 분류기로 위치 문제를 억지로 풀면 배경과 작은 물체를 구분하지 못한다. 여러 속성이 동시에 필요한 상품·의료 영상은 다중 레이블이나 계층 분류를 고려하며, 레이블 간 상호 배타성 가정을 데이터 정의에서 확인한다.

사전학습 모델 선택은 공개 정확도만 보지 않고 입력 해상도, 파라미터와 연산량, 메모리, 라이선스와 실제 하드웨어 지연을 비교한다. 작은 모델이 대량 엣지 장치에서 더 나은 전체 시스템이 될 수 있고, 큰 모델은 적은 라벨에서 전이가 유리할 수 있다. 운영 환경의 사진을 사용한 선형 탐침과 미세조정 파일럿으로 후보를 줄인다. 잘못된 예측을 사람이 쉽게 수정할 수 있는 인터페이스와 새 범주를 거부하는 경로가 있다면 같은 모델 성능에서도 실제 위험이 낮아진다.`,
  'neural-network': `**구조를 과제에 맞추기**

표 형태의 소규모 데이터에서는 트리 기반 모델과 선형 모델이 강한 기준선이 될 수 있으며 신경망이 항상 우월하지 않다. 이미지의 지역적 패턴에는 합성곱, 순서와 긴 관계에는 어텐션, 그래프 관계에는 메시지 전달 구조처럼 데이터의 대칭성과 연결을 반영한 구조가 표본 효율을 높인다. 범용 구조를 사용하더라도 입력 표현, 출력 제약과 손실 함수는 과제에 맞춰야 한다. 복잡한 모델을 선택할수록 하이퍼파라미터 탐색과 디버깅, 서빙 비용이 늘어난다.

전이 학습은 큰 데이터에서 배운 표현을 작은 목표 데이터에 활용한다. 기반 모델을 고정하고 출력층만 학습하면 빠르고 과적합이 적지만 분포가 다르면 표현을 충분히 바꾸지 못한다. 일부 또는 전체 층을 미세조정하면 적응력이 커지는 대신 작은 학습률, 더 많은 메모리와 회귀 검증이 필요하다. 어느 층을 풀지, 차등 학습률을 쓸지, 언제 조기 종료할지는 검증 데이터로 결정한다. 학습 데이터가 매우 적으면 모델 확대보다 데이터 품질과 레이블 지침 개선이 더 큰 이익을 줄 수 있다.`,
  embedding: `**임베딩 사용이 적합한 지점**

정확한 키, 숫자, 날짜와 법률 조항 번호를 찾는 문제는 문자열·희소 검색이 중요한 기준선이다. 표현이 달라도 의미가 비슷한 문서, 이미지와 문장의 교차 검색에는 학습 임베딩이 유용하다. 실제 검색은 두 점수를 결합해 후보를 만들고 더 비싼 재순위 모델로 순서를 다듬을 수 있다. 분류에서는 임베딩을 고정 특성으로 사용해 작은 선형 모델을 학습하면 적은 데이터로 빠른 기준선을 만들 수 있으며, 충분하지 않을 때만 전체 모델 미세조정을 검토한다.

개인화 임베딩은 사용자 행동을 압축하지만 민감한 속성과 선호를 추론할 수 있어 수집 목적, 보존 기간과 접근 권한이 필요하다. 추천에서 가까움은 기존 행동을 반복해 다양성과 새로운 항목 노출을 줄일 수 있다. 검색과 추천 모두 관련성 외에 최신성, 권위, 안전, 접근 권한을 별도 필터와 순위 신호로 적용한다. 임베딩 하나에 모든 정책을 학습시키려 하지 말고 감사 가능한 규칙과 결합한다. 모델이 바뀌면 점수 분포가 달라져 기존 임계값과 모니터링 기준도 다시 보정한다.`,
  guardrail: `**위험에 비례한 통제 선택**

정보 제공 챗봇은 근거 표시, 유해 출력 필터와 신고 경로가 중심이 될 수 있지만 외부 시스템을 조작하는 에이전트는 권한 분리, 인자 검증, 승인과 실행 감사가 더 중요하다. 공개 입력을 받는 서비스는 프롬프트 주입과 자원 고갈을, 내부 문서 서비스는 접근 권한 누출과 데이터 보존을 우선한다. 동일한 필터 묶음을 모든 제품에 복사하지 않고 자산과 공격 경로에 맞춰 통제를 고른다. 위험이 작은 영역에서 과도한 차단을 줄이는 것 역시 신뢰 가능한 시스템의 일부다.

구매한 안전 모델이나 외부 필터도 자신의 언어, 정책과 트래픽에서 평가해야 한다. 제공자의 범주 정의가 조직 정책과 다를 수 있고 버전 변경으로 임계값이 움직일 수 있다. 차단 전용, 점수 제공, 텍스트 수정 중 어떤 동작인지 확인하고 실패하거나 시간 초과될 때 허용할지 차단할지 정한다. 고위험 경로는 필터 장애 시 안전하게 닫고, 저위험 경로는 제한된 기능으로 낮출 수 있다. 사람 검토 팀의 용량과 교육 없이 모든 모호한 사례를 보내면 대기와 형식적 승인만 늘어난다.`,
  pretraining: `**사전학습과 다른 적응 방식 비교**

처음부터 사전학습하는 것은 데이터와 계산, 전문 운영 인력이 많이 필요해 대부분의 조직에는 기존 기반 모델을 활용하는 편이 현실적이다. 도메인 적응 사전학습은 관련 비라벨 텍스트로 학습 목표를 이어 가며 용어와 문체를 익히지만, 데이터 혼합과 학습률이 잘못되면 일반 능력이 퇴행할 수 있다. 지도 미세조정은 입력·정답 쌍으로 원하는 과제를 직접 가르치고, 검색 증강은 가중치를 바꾸지 않고 최신·사내 지식을 제공한다. 프롬프트와 도구 연결도 더 낮은 비용의 기준선이다.

선택은 지식의 변경 빈도, 라벨 가용성, 행동 형식과 배포 제약으로 결정한다. 자주 바뀌는 사실을 파라미터에 넣기보다 검색하는 편이 갱신과 출처 추적에 유리하다. 반복되는 전문 언어와 패턴이 방대한 비라벨 데이터에 있고 여러 과제에 공유된다면 도메인 사전학습의 가치가 커질 수 있다. 제한된 출력 형식과 업무 규칙은 지도 데이터나 구조화된 검증이 더 직접적이다. 각 단계의 추가 이익을 같은 보류 세트에서 측정하고, 비용과 안전 퇴행을 함께 비교한다.`,
  'query-key-value': `**어텐션 변형을 고르는 기준**

인코더의 양방향 자기 어텐션은 입력 전체를 함께 이해하는 분류와 표현 학습에 적합하고, 디코더의 인과 자기 어텐션은 미래 토큰을 가려 생성 확률을 올바르게 만든다. 인코더-디코더의 교차 어텐션은 번역이나 요약에서 출력 위치가 입력 표현을 조회하게 한다. 긴 문맥의 계산이 문제라면 국소·희소 패턴, 저랭크 근사, 상태 공간 모델과 메모리 검색 등 대안을 비교하되, 필요한 장거리 의존성과 실제 하드웨어 효율을 평가한다.

생성 추론에서는 과거 키와 값을 KV 캐시에 저장해 매 토큰마다 다시 계산하지 않는다. 다중 쿼리 어텐션은 여러 쿼리 헤드가 키·값 헤드를 공유하고, 그룹 쿼리 어텐션은 그 중간 형태로 캐시 크기와 메모리 대역폭을 줄인다. 공유가 많아지면 표현 용량과 품질에 영향을 줄 수 있으므로 모델 학습부터 설계되거나 적절한 변환이 필요하다. 단순히 실행 옵션으로 헤드 수를 바꿀 수 있는 것은 아니다. 서빙에서는 문맥 길이별 품질, KV 메모리와 토큰 지연을 함께 비교한다.`,
};
