export const W22_CORE_EXPANSIONS_3 = {
  'neural-network': {
    mechanism: `**연결된 함수의 합성과 학습**

신경망은 입력을 여러 층의 함수에 차례로 통과시켜 출력을 만드는 파라미터화된 모델이다. 기본 뉴런은 입력 벡터와 가중치의 내적에 편향을 더하고 비선형 활성 함수를 적용한다. 선형 변환만 여러 번 쌓으면 하나의 선형 변환과 같아 복잡한 결정 경계를 만들 수 없으므로 ReLU, sigmoid, GELU 같은 비선형성이 필요하다. 층이 깊어지면 앞선 층의 단순한 특징을 뒤의 층이 조합해 계층적 표현을 만들 수 있다. 합성곱망, 순환망, Transformer도 연결 방식과 연산이 다른 신경망 구조로 볼 수 있다.

지도학습에서는 모델 출력과 정답의 차이를 손실 함수로 계산하고 역전파로 각 파라미터에 대한 그래디언트를 구한다. 연쇄 법칙은 뒤쪽 연산의 미분을 앞쪽으로 전달하며, 최적화기는 그래디언트와 학습률을 사용해 가중치를 갱신한다. 미니배치는 여러 표본의 그래디언트를 묶어 계산 효율과 추정 안정성을 조절한다. 학습률이 너무 크면 손실이 발산하거나 좋은 지점을 지나치고, 너무 작으면 학습이 느리거나 평평한 영역에 머문다. 손실 감소가 실제 일반화를 의미하는지 확인하려면 학습에 쓰지 않은 검증 데이터가 필요하다.`,
    structure: `**층·파라미터·계산 그래프**

입력층이라는 표현은 데이터를 받는 모양을 가리키고, 은닉층은 중간 표현을, 출력층은 과제에 맞는 로짓이나 값을 만든다. 각 층의 너비는 표현 차원과 계산량을, 깊이는 함수 합성의 단계와 그래디언트 경로를 바꾼다. 잔차 연결은 입력을 몇 개 층 뒤의 출력에 더해 깊은 네트워크의 최적화를 돕고, 정규화 층은 활성값의 척도를 안정화한다. 드롭아웃은 학습 중 일부 활성값을 무작위로 끄지만 평가 모드에서는 전체를 사용하므로 모드 전환을 빠뜨리면 결과가 틀어진다.

PyTorch의 nn.Module은 파라미터와 하위 모듈을 등록하고 순전파 계산을 구성한다. 자동미분 시스템은 실행된 연산 그래프를 추적해 그래디언트를 계산한다. 파라미터, 버퍼, 일반 속성은 저장과 장치 이동에서 다르게 처리되므로 학습되지 않는 통계도 올바르게 등록해야 한다. 가중치 체크포인트에는 아키텍처 코드, 파라미터 이름과 형상이 맞아야 하며, 최적화기 상태와 난수 상태까지 저장해야 중단 지점에서 학습을 재현하기 쉽다. 혼합 정밀도는 속도와 메모리를 개선하지만 손실 스케일과 수치 범위를 관리해야 한다.`,
    limitations: `**일반화와 최적화의 실패**

파라미터가 많은 신경망은 훈련 데이터를 외울 수 있어 낮은 훈련 손실만으로 새 데이터 성능을 보장하지 못한다. 데이터 증강, 가중치 감쇠, 조기 종료, 드롭아웃은 과적합을 줄이는 수단이지만 잘못된 데이터 분할이나 누출을 해결하지는 않는다. 학습과 배포의 분포가 다르면 검증 성능이 유지되지 않으며, 배경 상관관계와 레이블 오류를 지름길로 배울 수 있다. 모델 크기를 늘리는 일은 데이터 대표성과 과제 정의를 대신하지 못한다.

깊은 계산에서는 그래디언트가 매우 작아지거나 커지는 소실·폭주가 발생한다. 초기화, 활성 함수, 정규화와 잔차 연결이 이를 완화하지만 모든 구조와 데이터에 같은 설정이 맞는 것은 아니다. 비볼록 손실에서는 최적화 결과가 초기값과 미니배치 순서에 따라 달라지며 동일한 코드도 하드웨어 커널의 비결정성 때문에 약간 다른 값을 낼 수 있다. 신경망은 내부 표현이 분산되어 단일 뉴런의 의미로 전체 결정을 설명하기 어렵다. 설명 기법은 모델 행동을 조사하는 보조 도구이지 인과적 근거의 보증으로 사용하지 않는다.`,
    practice: `**학습 실험과 오류 진단**

작은 표본 하나를 거의 완벽히 맞출 수 있는지 먼저 확인하면 데이터, 손실, 그래디언트 연결의 기본 오류를 찾을 수 있다. 그다음 단순한 선형 모델이나 얕은 네트워크를 기준선으로 두고 층과 파라미터를 점진적으로 늘린다. 매 실험에서 데이터 버전, 분할, 전처리, 초기화 시드, 아키텍처, 손실, 최적화기와 학습률 일정을 기록한다. 학습·검증 손실, 과제 지표, 그래디언트와 가중치 노름을 함께 그려 과적합, 발산, 죽은 활성값을 구분한다.

형상 검증과 유한값 검사를 각 주요 경계에 넣고, 작은 수치 예제로 자동미분 그래디언트를 유한 차분과 비교할 수 있다. 체크포인트를 다시 불러 같은 입력 결과가 일치하는지 시험하고, 평가 모드와 추론 전처리를 실제 배포 코드에서 확인한다. 여러 시드의 평균과 분산을 보고 우연한 초기화 이득을 피한다. 배포 후에는 입력 특성과 예측 분포, 지연, 사람 수정률을 감시하며 새 실패를 회귀 세트로 만든다. 모델 구조 변경은 데이터나 평가 변경과 분리해 원인을 추적한다.`,
  },
  embedding: {
    mechanism: `**이산 대상을 연속 공간으로 옮기기**

임베딩은 단어, 문장, 이미지, 사용자처럼 이산적이거나 복잡한 대상을 고정 차원의 실수 벡터로 표현한다. 표현 공간의 가까움이 학습 과제에서 유용한 관계를 반영하도록 파라미터를 조정한다. word2vec의 skip-gram은 중심 단어에서 주변 단어를 예측하고, CBOW는 주변 문맥에서 중심 단어를 예측한다. 전체 어휘 확률 계산이 비싸기 때문에 음성 샘플링 같은 근사 목표를 사용한다. 그 결과 비슷한 문맥에 등장한 단어가 가까운 벡터를 갖지만, 가까움은 사전적 동의어만이 아니라 주제·문법·사회적 연관을 섞어 나타낼 수 있다.

신경망의 임베딩 층은 정수 인덱스에 대응하는 학습 가능한 행렬의 행을 조회한다. PyTorch의 Embedding은 어휘 크기와 차원을 가진 테이블이며 padding 인덱스를 고정하거나 희소 그래디언트를 사용할 수 있다. 문장 임베딩은 토큰 벡터를 단순 평균하거나 전용 풀링과 대조 학습으로 만든다. 대조 학습은 관련 쌍을 가깝게, 비관련 쌍을 멀게 하지만 어떤 쌍을 양성·음성으로 정하는지가 공간의 의미를 결정한다. 범용 임베딩이라고 불려도 검색, 분류, 군집 등 각 과제에서 적합성을 검증해야 한다.`,
    structure: `**차원·정규화·거리 함수**

임베딩 차원이 커지면 더 많은 정보를 표현할 여지가 있지만 메모리와 계산이 증가하고 모든 차원이 유용하다는 보장은 없다. 벡터의 크기와 방향 중 어디에 정보가 담기는지는 학습 목표에 달려 있다. 코사인 유사도를 사용할 때는 벡터를 L2 정규화하면 내적 검색과 순위가 같아지지만, 크기를 제거해서는 안 되는 모델에는 적용하지 않는다. 유클리드 거리, 내적, 코사인은 정규화 조건에 따라 서로 다른 이웃을 고른다. 모델 문서가 권장하는 거리 함수를 사용하고 실제 관련성 데이터로 확인한다.

검색 시스템에서는 원문을 임베딩 모델로 변환하고 벡터 인덱스에 식별자와 함께 저장한다. 쿼리도 호환되는 모델과 전처리로 벡터화해야 한다. 문서와 질의에 다른 접두사나 인코더를 요구하는 모델도 있으므로 단순히 차원이 같다고 혼합할 수 없다. 인덱스는 벡터 외에 원문 위치, 접근 권한, 모델 버전과 생성 시점을 메타데이터로 가진다. 근사 최근접 탐색은 전체 벡터를 정확히 비교하는 대신 후보를 빠르게 좁히며, 인덱스 파라미터로 회수율과 지연·메모리의 균형을 조절한다.`,
    limitations: `**의미 압축의 손실과 편향**

고정 길이 벡터는 긴 문서의 여러 주제, 부정, 숫자와 세부 관계를 압축하면서 정보를 잃는다. 표면적으로 비슷한 문장이 사실 관계를 반대로 말해도 가까울 수 있고, 동일 단어의 여러 의미가 문맥 표현에서 충분히 분리되지 않을 수 있다. 가까운 벡터는 관련 후보를 찾는 신호이지 답이 참이라는 증거가 아니다. 검색 뒤 원문을 다시 읽고 필요한 경우 재순위화와 근거 검증을 해야 한다. 문서 분할 크기와 겹침이 바뀌면 같은 모델도 검색 단위와 성능이 달라진다.

학습 말뭉치의 사회적 편향과 빈도 차이는 공간의 연관 관계에 나타날 수 있다. 특정 집단과 직업·감정의 거리가 실제 의사결정에 사용되면 차별을 강화할 수 있다. 여러 언어의 정렬 품질도 언어별 데이터 양에 따라 다르며, 코드·표·도메인 약어는 일반 텍스트 모델이 잘 표현하지 못할 수 있다. 모델 교체 뒤 기존 인덱스를 그대로 쓰면 서로 다른 좌표 공간을 섞게 된다. 양자화와 차원 축소는 효율을 높이지만 희귀한 관련 항목의 순위를 바꿀 수 있으므로 업무 지표로 검증한다.`,
    practice: `**임베딩 검색 평가**

실제 사용자의 질의와 관련 문서 쌍을 만들고 쉬운 음성뿐 아니라 주제가 비슷하지만 답은 아닌 어려운 음성을 포함한다. 모델, 분할 방식, 정규화와 거리 함수를 고정해 exact 검색에서 recall@k, MRR, nDCG 같은 순위 지표를 측정한다. 그다음 근사 인덱스를 추가해 exact 기준 대비 후보 회수율, 지연과 메모리를 비교한다. 전체 평균 외에 언어, 문서 길이, 최신성, 숫자 질의와 도메인별 결과를 나눈다. 임의 표본의 가까운 이웃을 사람이 읽어 모델이 어떤 유사성을 학습했는지 조사한다.

운영 아티팩트에는 임베딩 모델 리비전, 토크나이저, 입력 접두사, 차원, 정규화, 거리 함수와 인덱스 설정을 저장한다. 모델이나 전처리가 바뀌면 새 인덱스를 별도 구축하고 같은 질의를 양쪽에 재생한 뒤 원자적으로 전환한다. 문서 삭제와 접근 권한 변경이 벡터 인덱스에도 반영되는지 시험한다. 분포가 변하면 점수 임계값과 검색 깊이를 다시 보정하고, 검색 실패가 생성 모델의 답변 오류로 가려지지 않도록 검색 단계의 후보와 점수를 독립적으로 기록한다.`,
  },
  guardrail: {
    mechanism: `**다층 통제로 위험을 줄이기**

가드레일은 생성형 AI 시스템의 입력, 모델 호출, 도구 실행, 출력과 운영 과정에서 허용 범위를 검사하고 위험한 행동을 차단하거나 검토로 보내는 통제다. 하나의 필터가 아니라 정책, 분류기, 구조 검증, 권한, 샌드박스, 사람 승인과 모니터링의 조합으로 설계한다. 입력 단계는 명백한 악성 요청과 민감정보를 찾고, 출력 단계는 유해 콘텐츠와 비밀 노출, 형식 위반을 검사한다. 도구 단계는 실제 실행될 행동과 인자를 허용 목록과 권한 정책에 대조한다. 모델이 자연어로 안전하다고 설명했는지와 상관없이 독립적인 실행 계층이 정책을 강제해야 한다.

가드레일 판정은 오류 비용에 따라 차단, 수정 요청, 안전한 대안, 사람 검토, 로깅으로 이어진다. 모든 의심 입력을 막으면 정상 사용을 크게 해치고, 임계값을 낮추면 위험한 사례를 놓친다. 위험 범주별로 재현율과 정밀도의 우선순위를 정하고 불확실 구간을 사람에게 보낸다. NIST의 생성형 AI 프로필처럼 위험을 수명주기에서 식별·측정·관리하고, 정책의 소유자와 재검토 조건을 둔다. 기술 필터는 사용 목적의 정당성, 데이터 거버넌스와 사건 대응을 대신하지 않는다.`,
    structure: `**신뢰 경계와 정책 엔진**

시스템 명령, 개발자 설정, 사용자 입력, 검색 문서와 도구 결과는 신뢰 수준이 다르다. 외부 문서 안의 지시는 데이터로 취급하고 상위 정책을 변경하지 못하게 해야 한다. OWASP가 설명하는 프롬프트 주입은 악성 입력이 모델의 지시 따르기 특성을 이용해 원래 목표를 바꾸는 문제이며, 문자열 금지 목록만으로 우회 표현과 간접 주입을 막기 어렵다. 모델에게 비밀을 제공한 뒤 “출력하지 말라”고 지시하는 방식보다 비밀을 모델 문맥에서 격리하고 필요한 도구가 제한적으로 사용하게 한다.

정책 엔진은 주체, 대상 자원, 행동, 맥락을 기준으로 허용 여부를 계산하고 모델과 별도로 버전 관리한다. 구조화 출력은 JSON 스키마로 필드와 자료형을 제한하지만 값의 의미와 권한까지 검증해야 한다. 샌드박스는 파일·네트워크·프로세스 접근과 자원 사용을 제한하고, 고위험 쓰기에는 실행 전 변경 미리보기와 승인을 둔다. 로그에는 판정 규칙, 입력의 안전한 지문, 모델·정책 버전, 실제 실행 결과를 남기되 민감 원문은 최소화한다. 여러 계층이 같은 실패 모드에 의존하지 않도록 서로 다른 통제를 배치한다.`,
    limitations: `**우회·오탐·새로운 공격**

자연어의 표현 공간은 넓어 고정 규칙과 분류기는 철자 변형, 다른 언어, 인코딩, 장문 속 숨김에 우회될 수 있다. 공격자는 여러 차례 대화를 통해 허용된 조각을 결합하거나, 모델이 읽는 웹페이지와 파일에 지시를 넣을 수 있다. 특정 공격 세트에서 높은 차단률이 미래의 공격을 보장하지 않는다. 레드팀은 알려진 기법을 반복하는 것뿐 아니라 시스템의 도구, 권한, 데이터 흐름을 보고 새로운 경로를 탐색해야 한다. 차단 실패를 모델 문제로만 보지 말고 비밀 격리와 최소 권한으로 피해 범위를 줄인다.

오탐은 특정 언어와 집단, 안전 연구와 교육적 문맥을 불균형하게 막을 수 있다. 사용자가 이유를 알 수 없는 차단을 반복해서 받으면 우회 시도가 늘고 신뢰가 낮아진다. 가능한 범위에서 정책 범주와 수정 방법을 안내하고 이의 제기 경로를 제공한다. 가드레일 자체도 지연과 비용을 추가하고 별도 모델을 사용하면 그 모델의 데이터와 취약성이 생긴다. 로그와 사람 검토 과정에서 민감 정보가 다시 노출될 수 있다. 어떤 통제도 완전하지 않으므로 탐지, 피해 제한, 복구와 학습을 함께 설계한다.`,
    practice: `**위협 모델과 공격 시험**

먼저 보호할 자산, 신뢰할 수 없는 입력 경로, 모델이 가진 도구와 권한, 최악의 영향을 그린다. 정상 업무와 금지 행동을 정책 예시로 만들고 입력·출력·도구 단계별 책임을 정한다. 시험 세트에는 직접·간접 프롬프트 주입, 인코딩, 다국어, 장문, 권한 상승, 데이터 유출, 과도한 자원 사용과 정상 경계 사례를 포함한다. 차단률만 보지 말고 정상 허용률, 사람 검토량, 공격 성공 때의 실제 영향, 복구 시간을 측정한다.

정책과 분류기 임계값은 변경 이력과 승인자를 남기고 회귀 세트에서 비교한 뒤 배포한다. 일부 트래픽에서 차단을 적용하지 않는 그림자 평가로 오탐을 관찰할 수 있지만 고위험 행동은 그림자 모드로 실행하지 않는다. 운영 경보는 탐지 건수 폭증, 동일 주체의 반복 우회, 새로운 도구 호출 패턴과 비밀 접근을 연결한다. 사건이 발생하면 세션을 격리하고 토큰을 폐기하며 영향을 받은 외부 상태를 확인한 뒤 공격 변형을 회귀 세트에 추가한다. 정기적으로 권한과 예외 목록을 줄이고 더 이상 필요한지 재검토한다.`,
  },
  pretraining: {
    mechanism: `**대규모 데이터에서 표현을 먼저 학습하기**

사전학습은 특정 소규모 과제를 풀기 전에 대규모 데이터와 자기지도 또는 비지도 목표로 일반적인 표현과 파라미터 초기값을 학습하는 단계다. 언어 모델은 다음 토큰 예측, 가려진 토큰 복원, 손상된 입력 복원 같은 목표를 사용할 수 있다. BERT는 양방향 문맥에서 가려진 토큰을 예측해 표현을 학습하고, 과제별 출력층을 추가해 비교적 적은 변경으로 여러 자연어 처리 과제에 미세조정했다. 자기회귀 모델은 왼쪽 문맥에서 다음 토큰을 예측하며 생성에 자연스럽게 연결된다. 목표가 다르면 같은 데이터에서도 표현되는 정보와 추론 방식이 달라진다.

사전학습의 이점은 여러 과제에서 공유되는 통계 구조를 큰 데이터로 학습하고 적은 라벨 데이터로 이전할 수 있다는 점이다. 표현 학습 관점에서는 원시 입력의 유용한 변동 요인을 분리하고 이후 과제가 선형 또는 간단한 함수로 읽을 수 있게 만드는 것을 기대한다. 그러나 모든 정보를 보존하는 것과 특정 과제에 유리한 불변성을 만드는 것은 상충할 수 있다. 사전학습 분포와 목표 과제가 멀거나 잘못된 편향을 강하게 배운 경우 미세조정에 도움이 되지 않거나 해를 줄 수도 있다.`,
    structure: `**데이터·목표·규모의 파이프라인**

파이프라인은 원천 수집, 라이선스와 정책 검토, 언어·형식 식별, 품질 필터링, 개인정보와 중복 처리, 토큰화, 샤딩, 학습과 체크포인트 평가로 이어진다. 필터는 노이즈를 줄이지만 특정 방언·문체·집단을 과도하게 제거할 수 있으므로 제거율과 표본을 범주별로 감사한다. 중복 데이터는 자주 반복된 문장을 과도하게 학습시키고 벤치마크 오염과 암기 위험을 높인다. 문서 단위와 근접 중복을 함께 다루며, 데이터 계보와 필터 버전을 보존해야 나중에 특정 출처의 영향을 조사할 수 있다.

학습 계산은 데이터 양, 파라미터 수, 토큰 수와 하드웨어 효율의 균형을 가진다. 분산 학습은 데이터·텐서·파이프라인 병렬화를 조합하고 통신과 장애 복구를 관리한다. 체크포인트에는 모델, 최적화기, 학습률 일정과 데이터 진행 위치가 필요하다. 학습 손실이 감소해도 능력과 안전이 같은 속도로 개선되는 것은 아니므로 중간 체크포인트를 고정 평가에 비교한다. 토크나이저와 문맥 길이는 학습 시작 뒤 바꾸기 어렵고 언어별 효율에 큰 영향을 주므로 데이터 분석 단계에서 결정한다.`,
    limitations: `**데이터 불투명성과 이전의 한계**

웹 규모 말뭉치는 출처, 정확성, 동의와 권리 상태가 불균일하다. 필터를 거쳐도 개인정보, 저작물, 유해한 표현과 잘못된 사실이 남을 수 있다. 데이터 목록을 완전히 공개하기 어렵더라도 범주, 시점, 수집·제외 정책과 알려진 결손을 문서화해야 한다. 삭제 요청이나 법적 변경에 대응하려면 어떤 데이터 버전이 어떤 체크포인트에 사용되었는지 계보가 필요하다. 이미 학습된 파라미터에서 특정 자료의 영향을 완전히 제거하는 일은 단순 파일 삭제와 다르다.

사전학습 손실과 다운스트림 품질의 관계는 과제별로 다르며 규모 확대가 모든 언어와 집단에 같은 이득을 주지 않는다. 공개 벤치마크가 말뭉치에 들어가면 실제 일반화보다 높은 점수가 나올 수 있다. 대규모 계산에는 비용과 에너지, 하드웨어 집중의 문제가 있고 재현 가능한 독립 검토가 어려워진다. 미세조정이 사전학습의 편향과 암기를 완전히 지우지 못하며 새로운 과제에서 파국적 망각이 생길 수 있다. 모델을 공개하거나 배포할 때 가중치 접근 수준, 사용 제한과 위험 완화의 실효성을 따로 평가한다.`,
    practice: `**데이터와 체크포인트 감사**

학습 전에 목표 능력과 제외할 사용을 정하고 데이터 출처별 허용 근거, 기간, 언어, 품질과 민감도를 기록한다. 원본과 변환본의 해시, 필터 코드와 통계를 보존하고 각 단계에서 남고 제거된 표본을 층화 추출해 사람이 검토한다. 개인정보 탐지, 중복 제거, 벤치마크 오염 검사는 서로 다른 도구로 수행하며 완벽한 제거를 주장하지 않는다. 작은 규모의 파일럿 학습으로 토크나이저 효율, 손실 안정성, 데이터 혼합 비율과 평가 신호를 확인한 뒤 규모를 늘린다.

훈련 중에는 손실, 그래디언트, 처리 토큰, 하드웨어 오류와 데이터 샤드 위치를 관찰하고 일정 간격으로 불변 체크포인트를 만든다. 고정된 능력·안전 평가와 오염 가능성이 낮은 보류 세트를 함께 실행한다. 최종 선택은 학습 손실이 가장 낮은 시점이 아니라 여러 과제, 위험, 비용의 문턱으로 결정한다. 공개 문서에는 데이터와 계산, 아키텍처, 평가 설정, 알려진 제한을 남기고 후속 미세조정이 어떤 기반 체크포인트에서 시작했는지 연결한다. 데이터나 코드 변경 뒤에는 같은 이름을 덮어쓰지 않고 새 버전으로 추적한다.`,
  },
  'query-key-value': {
    mechanism: `**관련성을 계산하는 세 가지 투영**

어텐션에서 각 입력 표현은 서로 다른 학습 행렬을 거쳐 쿼리, 키, 값 벡터가 된다. 쿼리는 현재 위치가 찾는 정보의 관점, 키는 각 위치가 제공할 수 있는 정보의 색인, 값은 실제로 집계할 내용으로 이해할 수 있다. 쿼리와 모든 키의 내적을 계산하고 키 차원의 제곱근으로 나눈 뒤 softmax를 적용하면 합이 1인 가중치가 된다. 이 가중치로 값 벡터의 가중합을 구해 현재 위치의 출력 표현을 만든다. 스케일링은 차원이 커질 때 내적 크기가 커져 softmax가 지나치게 포화되는 것을 완화한다.

쿼리와 키가 같은 입력에서 나오면 자기 어텐션이고, 쿼리는 한 시퀀스에서 키와 값은 다른 시퀀스에서 오면 교차 어텐션이다. 디코더의 인과 마스크는 현재 위치가 미래 토큰의 키를 보지 못하게 한다. 패딩 마스크는 실제 토큰이 아닌 위치에 가중치가 가는 것을 막는다. 마스크 값과 형상이 잘못되면 실행은 되더라도 미래 정보 누출이나 패딩 오염이 생길 수 있다. 어텐션 가중치는 입력에 따른 동적 혼합 계수이며, 값 투영과 이후 층을 거치므로 가중치 하나만으로 모델의 최종 인과적 설명을 단정할 수 없다.`,
    structure: `**다중 헤드와 텐서 형상**

다중 헤드 어텐션은 표현 차원을 여러 부분 공간으로 나누어 각 헤드가 별도의 쿼리·키·값 투영과 관련성 패턴을 학습하게 한다. 헤드 출력을 이어 붙인 뒤 출력 투영으로 섞는다. 모델 차원은 보통 헤드 수로 나누어져 헤드 차원을 만들며, 배치·헤드·질의 길이·키 길이 축의 순서를 명확히 해야 한다. 쿼리 길이와 키 길이는 교차 어텐션에서 다를 수 있고, 값 차원도 구현 계약을 확인해야 한다. 브로드캐스팅되는 마스크가 의도한 배치와 헤드에 적용되는지 작은 예제로 검사한다.

PyTorch MultiheadAttention은 쿼리, 키, 값 입력과 마스크, 배치 축 설정을 받으며 조건이 맞으면 최적화된 scaled dot product attention 경로를 사용할 수 있다. 추론 전용 자기 어텐션에서 입력 형상, 평가 모드, 그래디언트 비활성화 같은 조건이 빠른 경로 선택에 영향을 줄 수 있다. 성능을 위해 어텐션 가중치 반환을 끄는 옵션도 있다. 라이브러리 버전마다 허용 마스크 자료형과 빠른 커널 조건이 달라질 수 있으므로 API 문서와 프로파일러를 확인한다. 출력 값이 맞는지와 특정 최적화가 실제 선택되었는지를 별도 시험한다.`,
    limitations: `**제곱 복잡도와 해석의 주의점**

전체 어텐션 점수 행렬은 질의 길이와 키 길이의 곱에 비례해 커진다. 긴 문맥에서는 점수와 중간 활성값 메모리가 병목이 되며, FlashAttention 같은 정확한 I/O 최적화와 희소·선형 근사 같은 구조 변경이 서로 다른 해법을 제공한다. 메모리 효율 커널은 수학적 결과를 거의 유지하면서 메모리 왕복을 줄이지만 지원 자료형과 마스크에 제한이 있을 수 있다. 근사 어텐션은 계산을 줄이는 대신 어떤 장거리 관계를 놓치는지 과제별 평가가 필요하다.

높은 어텐션 가중치를 모델이 그 토큰만을 “이유”로 사용했다는 설명으로 해석하면 안 된다. 잔차 연결, 여러 헤드와 층, 값 벡터, MLP가 함께 출력을 결정한다. 서로 다른 파라미터가 비슷한 출력과 다른 가중치 지도를 만들 수 있다. 패딩·인과 마스크의 부호, boolean 의미, 매우 작은 상수는 정밀도에서 다르게 동작할 수 있다. 모든 키가 마스킹된 행은 softmax에서 정의되지 않은 값이 생길 수 있다. 혼합 정밀도에서는 큰 로짓과 마스크가 overflow나 NaN을 만들지 검사한다.`,
    practice: `**작은 행렬로 확인하는 구현**

두세 토큰과 한 헤드의 작은 쿼리·키·값 행렬을 만들어 내적, 스케일링, 마스크, softmax, 값 가중합을 손으로 계산한다. 라이브러리 출력과 비교하고 각 단계의 형상을 단언한다. 인과 마스크를 적용했을 때 첫 토큰의 출력이 미래 토큰을 바꿔도 변하지 않는지 시험하면 정보 누출을 찾을 수 있다. 패딩 토큰의 값과 길이를 바꾸어 실제 토큰 출력이 유지되는지도 확인한다. 여러 배치와 교차 어텐션에서 질의·키 길이가 다른 경우를 별도 테스트한다.

실제 모델에서는 시퀀스 길이를 늘리며 지연, 최대 메모리와 커널 선택을 프로파일링한다. 일반 구현과 최적화 구현의 출력을 정한 허용 오차 안에서 비교하고 그래디언트도 작은 사례에서 확인한다. 어텐션 지도를 시각화할 때는 헤드와 층을 임의로 평균하기 전에 차이를 보존하고, 지도만으로 성능 결론을 내리지 않는다. 모델 리비전, 라이브러리, 자료형, 마스크 종류와 장치 정보를 실험 기록에 남긴다. 최적화 뒤에는 긴 문맥과 특수 마스크를 포함한 회귀 세트를 실행한다.`,
  },
};
