# AI Learning Wiki 업데이트 기록

버전별 변경 사항과 콘텐츠 최신성 검토 결과를 기록한다. 날짜는 한국 표준시(KST)를 기준으로 한다.

## v1.0.0

### 2026-08-01

- W59에서 맞춤 학습, 평가 불확실성, KV 캐시·분산 서빙, JSON Schema 의미 체계에 관한 신규 용어 24개를 검토 완료 백과 문서로 추가했다.
- 신규 문서는 4개 분야에 6개씩 배치하고, 각 문서를 AI 기초, LLM 평가와 관측성, LLMOps와 프로덕션 서빙, 프롬프트 시스템 설계 코스의 선택 심화 단계와 연결했다.
- 맞춤 학습 경로 생성기, 평가 지표 실험실, 모델 메모리·KV 캐시·문맥 계산기, 프롬프트·JSON Schema 검증기에도 관련 문서를 6개씩 연결해 개념 설명과 실습 사이의 이동 경로를 보강했다.
- 24개 문서의 240개 섹션 주장과 126개 출처를 해시로 고정하고, 문서·코스·도구·전체 색인 경로를 W59 전용 검증과 렌더 검사에 포함했다.
- 검토 완료 백과 문서는 총 1,624개가 되었으며 README의 콘텐츠 현황과 기존 성장형 검증 규칙을 최신 수량에 맞췄다. 전체 빌드에서 1,671개 정적 페이지와 82,965개 내부 링크를 검증했다.

### 2026-07-30

- W58에서 프롬프트 변수의 선언·사용 불일치와 JSON Schema Draft 2020-12 자체 오류를 검사하는 프롬프트·JSON Schema 검증기를 공개했다.
- 예시 출력 JSON의 필수 필드·타입·추가 속성 제약 위반을 경로별로 구분하고, 원문을 제외한 보고서만 선택적으로 복사할 수 있다.
- 프롬프트와 입력 JSON은 브라우저에서만 처리하며 URL·브라우저 저장소·서버에 남기지 않는다.
- W58의 10개 결정론 고정 입력, 공통 세션 스키마, 접근성·모바일 렌더와 총 1,647개 정적 페이지·81,686개 내부 링크를 자동 검증했다.

### 2026-07-29

- W54에서 AI 실험실의 정보 구조, 4개 영역, 6개 도구 계획과 공통 도구·세션·등록부 데이터 계약을 확정했다.
- W55에서 `/lab/` 허브와 첫 활성 도구인 맞춤 학습 경로 생성기를 공개했다.
- W56에서 혼동행렬로 정확도·정밀도·재현율·특이도·F1 점수를 계산하는 평가 지표 실험실을 공개했다.
- W57에서 가중치·Adam 계열 학습 상태·KV 캐시를 분리하고 문맥 길이에 따른 캐시 증가를 비교하는 모델 메모리·KV 캐시·문맥 계산기를 공개했다.
- 모델 크기·정밀도·실행 모드·레이어·배치·문맥·KV 헤드·헤드 차원을 URL로 공유하며, 계산에서 제외한 활성값·작업 공간·프레임워크 오버헤드를 결과에 명시한다.
- W57의 8개 결정론 고정 입력, 문맥 길이 선형 증가, 공통 세션 스키마, 접근성·모바일 스타일과 총 1,646개 정적 페이지·79,996개 내부 링크를 자동 검증했다.
- 분모 0은 0점이 아닌 계산 불가로 구분하고, 실제 클래스 수가 4배 이상 차이 나면 불균형을 알린다. 점수 분포가 없는 임곗값 입력은 변경 방향만 설명하며 새 지표를 추정하지 않는다.
- W56의 8개 결정론 고정 입력, 공통 세션 스키마, 접근성·모바일 스타일과 총 1,645개 정적 페이지를 전체 빌드에서 검증했다.
- 1,600개 백과 문서의 선수 관계를 목표·수준·최대 분량·학습 초점·수학 포함 여부에 따라 탐색하고, 권장 순서·선정 이유·코스 연결·선수 관계 충족률을 표시한다.
- 모든 AI 실험실 계산은 브라우저에서만 수행하며 입력을 전송하거나 저장하지 않는다. W55~W57의 선택 조건은 URL로 공유할 수 있다.
- 5개 결정론 고정 입력, 공통 세션 스키마, 접근성·모바일 스타일, 1,644개 정적 페이지와 배포 경로를 전체 빌드에서 검증했다.

### 2026-07-26

- 공식 서비스 주소를 `https://ai-wiki.kr/`로 전환하고 Astro canonical·manifest·정적 데이터 경로를 사용자 지정 도메인의 루트 기준으로 통일했다.
- GitHub Actions와 배포 검증기를 루트 경로 배포에 맞게 조정하고, 기존 GitHub Pages 프로젝트 하위 경로 의존성을 제거했다.

### 2026-07-23

- 1,600개 백과 문서 중 변동 가능성이 높은 모델·프레임워크·프로토콜 문서를 공식 1차 자료 중심으로 선별 점검했다.
- **토큰 효율** 문서에 2026년 7월 21일 공개된 Gemini 3.6 Flash의 공급자 보고 비교 사례를 추가하고, 동일 조건 재측정이 필요하다는 해석 한계를 명시했다.
- **모델 컨텍스트 프로토콜**과 **MCP 전송 계층** 문서에 2025-11-25 안정 규격과 2026-07-28 릴리스 후보를 구분하고, 후보 규격의 상태 비저장 전환과 호환성 주의점을 추가했다.
- **PyTorch** 문서에 현재 최신 안정 버전 2.13.0과 주요 기능·호환성 검토 항목을 반영했다.
- 최근 1주 동안 공개된 연구를 점검했으나, 기존 백과 정의를 바꿀 만큼 검증·정착된 새 이론은 확인되지 않아 단기 프리프린트는 편입하지 않았다.
- 대문, 검색·자료실, 학습 코스, 분류·백과 생성 템플릿, 개인정보 처리방침과 이용약관의 공식 문체를 ‘~다/~이다’ 체로 통일했다.
- README의 콘텐츠 기준일과 정책 문서의 최종 업데이트 날짜를 2026년 7월 23일로 갱신했다.

### 2026-07-16 기준선

- 검토 완료 백과 문서 1,600개, 학습 코스 16개, 프롬프트 1,500개, 코드·설정 자료 120개로 v1.0.0 기준선을 확정했다.
- 위키형 하이퍼링크 탐색, 전체 문서 색인, 통합 검색, 학습 코스와 자료실을 GitHub Pages 배포 구조로 구성했다.
